import MovieFilter from './MovieFilter';

function Filters() {
  return (
    <section className="filters">
      <form>
        <MovieFilter />
      </form>
    </section>
  );
}

export default Filters;
